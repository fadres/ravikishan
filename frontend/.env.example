# Ravikishan frontend environment template.
# Copy to .env.local and adjust. In production set VITE_API_URL on
# Cloudflare Pages / Vercel in the dashboard (build-time env var).

# Backend API base URL (dev default points at the local Express server).
VITE_API_URL="http://localhost:4000"
