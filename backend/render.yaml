# Render blueprint for the Ravikishan backend web service.
# Create the free PostgreSQL database separately on Neon or Supabase and
# paste its connection string as DATABASE_URL (see docs/README.md).

services:
  - type: web
    name: ravikishan-api
    runtime: node
    plan: free
    buildCommand: npm install && npx prisma generate
    startCommand: npm run migrate && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_ACCESS_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: CORS_ORIGIN
        sync: false
      - key: OWNER_EMAIL
        value: harindarsah98172@gmail.com
      - key: OWNER_PASSWORD
        sync: false
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: MAIL_FROM
        sync: false
      - key: SITE_URL
        value: https://ravikisan.netlify.app
      - key: R2_ENDPOINT
        sync: false
      - key: R2_ACCESS_KEY_ID
        sync: false
      - key: R2_SECRET_ACCESS_KEY
        sync: false
      - key: R2_BUCKET
        sync: false

# Environment variables (set in Render dashboard)
# DATABASE_URL must be set to your PostgreSQL database connection string
# Example: postgresql://username:password@hostname:5432/database_name
# Leave DATABASE_URL empty in this file and set it in the Render dashboard
